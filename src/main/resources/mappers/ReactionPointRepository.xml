<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.demo.repository.ReactionPointRepository">

	<select id="getSumReactionPoint">
		SELECT IFNULL(SUM(rp.point), 0)
		FROM `reactionPoint`
		rp
		WHERE rp.relTypeCode = #{relTypeCode} and rp.relId = #{relId} and
		rp.memberId = #{loginedMemberId}
	</select>

	<insert id="increaseReactionPoint">
		INSERT INTO `reactionPoint`
		SET `regDate` = NOW(),
		`updateDate` = NOW(),
		`memberId` = #{loginedMemberId},
		`relTypeCode` =
		#{relTypeCode},
		`relId` = #{relId},
		`point` = 1
	</insert>

	<delete id="deleteReactionPoint">
		DELETE FROM `reactionPoint`
		WHERE `memberId` = #{loginedMemberId}
		AND `relTypeCode` = #{relTypeCode}
		AND `relId` = #{relId}
	</delete>

</mapper>





